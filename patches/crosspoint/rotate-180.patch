diff --git a/src/CrossPointSettings.cpp b/src/CrossPointSettings.cpp
index 739580a..e89f191 100644
--- a/src/CrossPointSettings.cpp
+++ b/src/CrossPointSettings.cpp
@@ -24,7 +24,7 @@ void readAndValidate(FsFile& file, uint8_t& member, const uint8_t maxValue) {
 #if CROSSPOINT_PAPERS3
 bool CrossPointSettings::normalizePaperS3Orientation() {
   if (!isPaperS3OrientationSupported(orientation)) {
-    orientation = PORTRAIT;
+    orientation = INVERTED;
     return true;
   }
   return false;
diff --git a/src/CrossPointSettings.h b/src/CrossPointSettings.h
index 7422808..f86b53d 100644
--- a/src/CrossPointSettings.h
+++ b/src/CrossPointSettings.h
@@ -67,16 +67,21 @@ class CrossPointSettings {
   };
 
 #if CROSSPOINT_PAPERS3
+  // This device is physically mounted 180° from the panel's stock
+  // orientation (see docs/hardware-orientation.md) — INVERTED, not PORTRAIT,
+  // is this device's "normal" portrait. Still only 2 of the 4 possible
+  // orientations are supported here, same as before; only which one counts
+  // as "portrait" changed.
   static bool isPaperS3OrientationSupported(uint8_t orientation) {
-    return orientation == PORTRAIT || orientation == LANDSCAPE_CCW;
+    return orientation == INVERTED || orientation == LANDSCAPE_CCW;
   }
 
   static uint8_t normalizePaperS3Orientation(uint8_t orientation) {
-    return orientation == LANDSCAPE_CCW ? LANDSCAPE_CCW : PORTRAIT;
+    return orientation == LANDSCAPE_CCW ? LANDSCAPE_CCW : INVERTED;
   }
 
   static uint8_t nextPaperS3Orientation(uint8_t orientation) {
-    return orientation == LANDSCAPE_CCW ? PORTRAIT : LANDSCAPE_CCW;
+    return orientation == LANDSCAPE_CCW ? INVERTED : LANDSCAPE_CCW;
   }
 #endif
 
@@ -184,7 +189,7 @@ class CrossPointSettings {
   uint8_t shortPwrBtn = IGNORE;
   // EPUB reading orientation settings
   // 0 = portrait (default), 1 = landscape clockwise, 2 = inverted, 3 = landscape counter-clockwise
-  uint8_t orientation = PORTRAIT;
+  uint8_t orientation = INVERTED;
   // Button layouts (front layout retained for migration only)
   uint8_t frontButtonLayout = BACK_CONFIRM_LEFT_RIGHT;
   uint8_t sideButtonLayout = PREV_NEXT;
diff --git a/src/activities/Activity.cpp b/src/activities/Activity.cpp
index 51800dd..5862395 100644
--- a/src/activities/Activity.cpp
+++ b/src/activities/Activity.cpp
@@ -13,7 +13,9 @@ void Activity::onEnter() {
   if (!isReaderActivity()) {
     // Non-reader activities always render in portrait so footer buttons match
     // the physical bottom of the device where drawButtonHints draws them.
-    renderer.setOrientation(GfxRenderer::Orientation::Portrait);
+    // PortraitInverted (not Portrait) is this device's fixed 180° mount —
+    // see docs/hardware-orientation.md for why.
+    renderer.setOrientation(GfxRenderer::Orientation::PortraitInverted);
   }
   mappedInput.setTouchOrientation(renderer.getOrientation());
   // Enable footer nav buttons for all non-reader activities; readers use full screen for content
diff --git a/src/activities/boot_sleep/SleepActivity.cpp b/src/activities/boot_sleep/SleepActivity.cpp
index 06efdef..8309875 100644
--- a/src/activities/boot_sleep/SleepActivity.cpp
+++ b/src/activities/boot_sleep/SleepActivity.cpp
@@ -22,7 +22,7 @@ void SleepActivity::onEnter() {
   if (APP_STATE.lastSleepFromReader) {
     ReaderUtils::applyOrientation(renderer, SETTINGS.orientation);
     GUI.drawPopup(renderer, tr(STR_ENTERING_SLEEP));
-    renderer.setOrientation(GfxRenderer::Orientation::Portrait);
+    renderer.setOrientation(GfxRenderer::Orientation::PortraitInverted);
   } else {
     GUI.drawPopup(renderer, tr(STR_ENTERING_SLEEP));
   }
diff --git a/src/activities/reader/EpubReaderActivity.cpp b/src/activities/reader/EpubReaderActivity.cpp
index 727bc9b..5c4a3cd 100644
--- a/src/activities/reader/EpubReaderActivity.cpp
+++ b/src/activities/reader/EpubReaderActivity.cpp
@@ -97,9 +97,10 @@ void EpubReaderActivity::onEnter() {
 void EpubReaderActivity::onExit() {
   Activity::onExit();
 
-  // Reset orientation back to portrait for the rest of the UI
-  renderer.setOrientation(GfxRenderer::Orientation::Portrait);
-  mappedInput.setTouchOrientation(CrossPointSettings::PORTRAIT);
+  // Reset orientation back to the rest of the UI's fixed portrait mount —
+  // see docs/hardware-orientation.md.
+  renderer.setOrientation(GfxRenderer::Orientation::PortraitInverted);
+  mappedInput.setTouchOrientation(CrossPointSettings::INVERTED);
 
   APP_STATE.readerActivityLoadCount = 0;
   APP_STATE.saveToFile();
diff --git a/src/activities/reader/TxtReaderActivity.cpp b/src/activities/reader/TxtReaderActivity.cpp
index 399060a..e88be75 100644
--- a/src/activities/reader/TxtReaderActivity.cpp
+++ b/src/activities/reader/TxtReaderActivity.cpp
@@ -48,9 +48,9 @@ void TxtReaderActivity::onEnter() {
 void TxtReaderActivity::onExit() {
   Activity::onExit();
 
-  // Reset orientation back to portrait for the rest of the UI
-  renderer.setOrientation(GfxRenderer::Orientation::Portrait);
-  mappedInput.setTouchOrientation(CrossPointSettings::PORTRAIT);
+  // Reset orientation back to the fixed inverted-portrait mount for the rest of the UI
+  renderer.setOrientation(GfxRenderer::Orientation::PortraitInverted);
+  mappedInput.setTouchOrientation(CrossPointSettings::INVERTED);
 
   pageOffsets.clear();
   currentPageLines.clear();
diff --git a/src/activities/reader/XtcReaderActivity.cpp b/src/activities/reader/XtcReaderActivity.cpp
index 64050d2..b6c4201 100644
--- a/src/activities/reader/XtcReaderActivity.cpp
+++ b/src/activities/reader/XtcReaderActivity.cpp
@@ -53,8 +53,8 @@ void XtcReaderActivity::onEnter() {
 void XtcReaderActivity::onExit() {
   Activity::onExit();
 
-  renderer.setOrientation(GfxRenderer::Orientation::Portrait);
-  mappedInput.setTouchOrientation(CrossPointSettings::PORTRAIT);
+  renderer.setOrientation(GfxRenderer::Orientation::PortraitInverted);
+  mappedInput.setTouchOrientation(CrossPointSettings::INVERTED);
 
   APP_STATE.readerActivityLoadCount = 0;
   APP_STATE.saveToFile();
diff --git a/src/components/themes/BaseTheme.cpp b/src/components/themes/BaseTheme.cpp
index d80c1e8..a2b95fc 100644
--- a/src/components/themes/BaseTheme.cpp
+++ b/src/components/themes/BaseTheme.cpp
@@ -128,7 +128,7 @@ void BaseTheme::drawProgressBar(const GfxRenderer& renderer, Rect rect, const si
 void BaseTheme::drawButtonHints(GfxRenderer& renderer, const char* btn1, const char* btn2, const char* btn3,
                                 const char* btn4) const {
   const GfxRenderer::Orientation orig_orientation = renderer.getOrientation();
-  renderer.setOrientation(GfxRenderer::Orientation::Portrait);
+  renderer.setOrientation(GfxRenderer::Orientation::PortraitInverted);
 
   const int pageHeight = renderer.getScreenHeight();
 #if CROSSPOINT_PAPERS3
diff --git a/src/components/themes/lyra/LyraTheme.cpp b/src/components/themes/lyra/LyraTheme.cpp
index 25ba794..b33a04f 100644
--- a/src/components/themes/lyra/LyraTheme.cpp
+++ b/src/components/themes/lyra/LyraTheme.cpp
@@ -336,7 +336,7 @@ void LyraTheme::drawList(const GfxRenderer& renderer, Rect rect, int itemCount,
 void LyraTheme::drawButtonHints(GfxRenderer& renderer, const char* btn1, const char* btn2, const char* btn3,
                                 const char* btn4) const {
   const GfxRenderer::Orientation orig_orientation = renderer.getOrientation();
-  renderer.setOrientation(GfxRenderer::Orientation::Portrait);
+  renderer.setOrientation(GfxRenderer::Orientation::PortraitInverted);
 
   const int pageHeight = renderer.getScreenHeight();
 #if CROSSPOINT_PAPERS3
